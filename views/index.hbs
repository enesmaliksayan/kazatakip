<h1>{{title}}</h1>
<ul>
{{#each namaz}}
    <li style="list-style: none;font-weight: bold ;padding:15px;margin:15px">
        <label style="margin-right:20px">{{this.vakit}}:</label>
        <input type="number" value="{{this.kalan}}" id="{{this._id}}">
        <button onclick="guncelle('{{this._id}}')"> Güncelle</button>
        <button onclick="detay('{{this._id}}')"> Detay</button>
    </li>
{{/each}}
</ul>
